const leia = require("readline-sync")

let numerosPares = 0
let numerosImpares = 0

leia.question("Digite 10 numeros inteiros: ")

for (contador = 0; contador <= 100; contador++ ){
console.log( "0" + contador + "Quantidade de numeros pares: " + numerosPares);


}
